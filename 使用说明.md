# 专升本英语学习系统 - 使用说明

## 🎯 功能特点

- 📖 **词汇学习** - 50章节，含音标、释义、近义词、例句、图片辅助记忆
- ✏️ **词汇练习** - 支持近义中文翻译输入，智能判题
- 📐 **语法学习** - 50章系统语法知识 + 练习
- ✒️ **应用文金句** - 50种应用文类型，开头/中间段/结尾金句
- 🔍 **阅读文库** - 20篇精选文章，自动标注3500词汇及翻译
- ❌ **错题本** - 自动收集错题，支持按章节再练习
- 📊 **学习仪表盘** - 进度统计、正确率分析
- 👤 **用户系统** - 注册登录，多设备数据同步

## 🚀 快速启动

### 方式一：本地模式（直接打开HTML）

双击 `index.html` 即可在浏览器中使用。

> **注意**：本地模式下无法使用登录和同步功能，数据保存在浏览器本地。

### 方式二：服务器模式（推荐，手机电脑都能用）

#### 1. 安装 Node.js

- 下载地址：https://nodejs.org/zh-cn/
- 下载 LTS 长期支持版，按提示安装即可
- 安装完成后，打开命令行输入 `node -v`，能看到版本号即安装成功

#### 2. 安装依赖

在项目文件夹的 `server` 目录下打开命令行，运行：

```bash
cd server
npm install
```

#### 3. 启动服务器

```bash
npm start
```

启动成功后会显示：

```
========================================
  专升本英语学习系统 - 服务器已启动
========================================

  本机访问:  http://localhost:3000
  局域网访问: http://<你的IP>:3000

  手机和电脑在同一WiFi下，用浏览器打开上面的地址即可使用

  按 Ctrl+C 停止服务器
========================================
```

## 📱 手机访问方法

### 前提条件
- 手机和电脑连接**同一个WiFi**
- 电脑防火墙允许3000端口访问

### 具体步骤

1. **查看电脑IP地址**
   - Windows：按 `Win+R`，输入 `cmd` 回车，再输入 `ipconfig`，找到 IPv4 地址（一般是 192.168.x.x）
   
2. **手机浏览器访问**
   - 打开手机浏览器，输入 `http://你的IP:3000`
   - 例如：`http://192.168.1.100:3000`

3. **添加到桌面（像APP一样用）**
   - **苹果手机**：Safari 打开 → 分享 → 添加到主屏幕
   - **安卓手机**：Chrome 打开 → 菜单 → 添加到主屏幕

## 👤 用户系统

### 注册账号
1. 点击右上角「登录」按钮
2. 切换到「注册」标签
3. 输入用户名（3-20位）和密码（至少6位）
4. 点击「注册并登录」

### 登录账号
1. 点击右上角「登录」按钮
2. 输入用户名和密码
3. 点击「登录」

登录后，学习进度和错题会自动同步到服务器，在不同设备上登录同一账号即可看到相同的数据。

## 📂 项目结构

```
├── index.html              # 主页面
├── css/
│   └── style.css           # 样式文件
├── js/
│   ├── app.js              # 主应用逻辑
│   └── data/
│       ├── vocabulary.js   # 词汇数据 1-10章
│       ├── vocabulary2.js  # 词汇数据 11-25章
│       ├── vocabulary3.js  # 词汇数据 26-35章
│       ├── vocabulary4.js  # 词汇数据 36-42章
│       ├── vocabulary5.js  # 词汇数据 43-50章
│       ├── grammar.js      # 语法数据 50章
│       ├── writing.js      # 应用文金句 50章
│       └── reading.js      # 阅读文库 20篇
└── server/
    ├── package.json        # 后端依赖配置
    ├── server.js           # 后端服务器代码
    └── data.db             # SQLite 数据库（自动生成）
```

## 💡 使用技巧

1. **词汇学习** - 点击单词旁的🔊可以朗读，点击🖼️可以查看记忆图片
2. **词汇练习** - 输入意思相近的中文翻译也算正确哦
3. **阅读文章** - 点击文章中高亮的蓝色单词可以查看释义
4. **错题本** - 做错的题自动收录，随时可以再练习巩固
5. **数据同步** - 登录后自动同步，切换设备不用怕丢失进度

## ⚠️ 常见问题

**Q: 手机访问不了怎么办？**
- 确认手机和电脑在同一个WiFi下
- 检查电脑防火墙是否阻止了3000端口
- 尝试关闭电脑防火墙测试一下

**Q: 忘记密码了怎么办？**
- 目前还没有找回密码功能，请牢记密码
- 或者注册一个新账号

**Q: 数据存在哪里？**
- 本地模式：存在浏览器的 localStorage 中
- 服务器模式：存在服务器的 SQLite 数据库（server/data.db）中

**Q: 怎么把网站部署到公网？**
- 可以部署到阿里云/腾讯云等云服务器
- 也可以使用内网穿透工具（如花生壳、ngrok）

## 🛠️ 技术栈

- 前端：HTML + CSS + JavaScript（原生）
- 后端：Node.js + Express
- 数据库：SQLite（better-sqlite3）
- 认证：JWT（jsonwebtoken）
- 密码加密：bcryptjs
